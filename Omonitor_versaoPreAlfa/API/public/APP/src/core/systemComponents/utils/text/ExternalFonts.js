import { externalFontsLinks } from "./externalFontsLinks.js";
import { addLinksToDOM } from "../../../utils/tools/addLinksToDOM.js";

export default class ExternalFonts{
    constructor(){
        
    }

    catamaran(){
        addLinksToDOM(externalFontsLinks[`Catamaran`]);
        return `"Catamaran", sans-serif`;
    }

    FiraSans(){
        addLinksToDOM(externalFontsLinks[`FiraSans`]);
        return `"Fira Sans", sans-serif`;
    }

    FacultyGlyphic(){
        addLinksToDOM(externalFontsLinks[`FacultyGlyphic`]);
        return `"Faculty Glyphic", sans-serif`;
    }

    JosefinSans(){
        addLinksToDOM(externalFontsLinks[`JosefinSans`]);
        return `"Josefin Sans", sans-serif`;
    }

    BlackOpsOne(){
        addLinksToDOM(externalFontsLinks[`BlackOpsOne`]);
        return`"Black Ops One"`;
    }

Audiowide(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);
    return `"Audiowide"`;
}

Boldonse(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Boldonse"`;
}

Catamaran(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Catamaran"`;
}

FacultyGlyphic(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Faculty Glyphic"`;
}

FiraSans(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Fira Sans"`;
}

JosefinSans(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Josefin Sans"`;
}

Jura(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Jura"`;
}

Kanit(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Kanit"`;
}

Lora(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Lora"`;
}

Orbitron(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Orbitron"`;
}

Oswald(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Oswald"`;
}

PinyonScript(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Pinyon Script"`;
}

PlaywriteATGuides(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Playwrite AT Guides"`;
}

PlaywriteDKLoopet(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Playwrite DK Loopet"`;
}

PlaywritePLGuides(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Playwrite PL Guides"`;
}

RobotoCondensed(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Roboto Condensed"`;
}

SmoochSans(){
    addLinksToDOM(externalFontsLinks[`fontLink`]);

    return `"Smooch Sans"`;
}
}